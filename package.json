{
  "name": "ui-design-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix gallery install --legacy-peer-deps && npm --prefix gallery run dev",
    "build": "node scripts/build-registry.mjs",
    "sync-rules": "bash scripts/sync-rules.sh",
    "sync-rules:poc": "bash scripts/sync-rules.sh --target",
    "build:all": "npm run build && npm run sync-rules",
    "create": "bash scripts/create-fission-ui.sh"
  },
  "description": "Fission UI private shadcn/ui registry"
}
